/**
 * Student + shared helpers for platform watermark (Admin: `GET /v1/feature`).
 */
export { getWatermarkSettings, parseWatermarkConfigFromFeatures } from './watermark-from-features';
export { buildWatermarkText } from './watermark-text';
