# 2026-08-03

- Continued the student exam audit. Added `src/lib/student-exam-score.ts` to centralize defensive percentage clamping, score/total conversion, and passing-mark percentage calculation. Applied it to student attempt cards, the post-submit result view, and Course Details so displayed percentages stay within 0–100 and zero totals do not divide.
- Added `reason_image` rendering to student answer review through the existing student exam media resolver, shown alongside translated textual feedback for selected or correct answers.
- Confirmed Course Details still uses only `course.attributes.exams`. The Student Home relevant-exams widget and secure three-mode access behavior remain backend-blocked pending an authenticated relevance-scoped list contract and explicit `access_mode`.
- Full TypeScript validation passed; targeted ESLint for the updated student exam files passed with zero errors and warnings.
- Ran a recursive locale-tree comparison and reconciled all English/Arabic missing translation paths. Both locale files now have identical leaf paths and value types.
