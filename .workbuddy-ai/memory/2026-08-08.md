# 2026-08-08

## Edit-chapter upload progress bar (departments pages)
- Task: show upload progress bar when editing a chapter, matching create-chapter behavior, in both `app/(doctor)/doctor/departments/page.tsx` and `app/(admin)/departments/page.tsx`.
- Root cause: data flow already existed (`useUpdateChapter` hook exposes `progress`; page passes `progress={selectedNode?.type === "chapter" ? updateChapterProgress : 0}` to `<EditModal>`). Only `EditModal` never rendered it, while `AddModal` did.
- Fix: mirrored the `AddModal` progress UI block into `EditModal` (before footer) and updated the submit button label to `Updating... ${progress}%` while uploading. `Loader2` already imported/used.
- Verified: `npm run typecheck` passes (exit 0).
